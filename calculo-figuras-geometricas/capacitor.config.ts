import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'calculo.figuras.geometricas',
  appName: 'calculo-figuras-geometricas',
  webDir: 'www'
};

export default config;
